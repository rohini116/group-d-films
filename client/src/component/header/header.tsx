import Logo from "./logo";
import Nav from "./nav";

const Header: React.FC = () => {
  return (
    <header>
     
      <Nav /> 
    </header>
  );
};

export default Header;

/* Move <Logo /> from above Nav */
